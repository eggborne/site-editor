{
  "rules": {
    "clients": {
      "$userId": {
        ".read": "$userId === auth.uid",
        ".write": "$userId === auth.uid"
      }
    },
    "preferences": {
      ".read": true,
      ".write": "auth != null"
    }
  }
}